import { Component } from "@angular/core";

@Component({
    selector: 'page-login', standalone: true,
    template: '<h2>Вхід в систему</h2><p>Сторінка "Вхід в систему" (заглушка)</p>'
}) export class LoginPage { }
