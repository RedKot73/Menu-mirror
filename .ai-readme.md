"Агент, ознакомься с содержимым файла .ai-readme.md и выполни задание из раздела [DEEP SCAN & UPDATE] для полной перезаписи файла .ai-context.md на основе анализа текущего кода."

📌 Permanent Project Essence (Core Context)
IMPORTANT: This section defines the project's identity. It must be used as the starting point for any generated version of .ai-context.md.

Project Overview: A specialized Mini-ERP system focused on document template management and automated generation.

Core Functionalities:

Data-Driven Generation: Automatically generating standard/typical documents (templates) based on accumulated business data.

Document Journal: A centralized registry for tracking, storing, and managing the history of all generated and uploaded documents.

Summary Statistics: Analytical dashboards and reporting derived from system-wide data accumulation.

Workflow & Logic: Heavy reliance on OpenXML for document processing, with business rules (BR) and data structures (DB) integrated into the generation engine.

🤖 [DEEP SCAN & UPDATE] PROMPT FOR AI AGENT
Task: Perform a comprehensive project-wide scan and REWRITE the '.ai-context.md' file from scratch. Your objective is to ensure '.ai-context.md' reflects the current state of the codebase. Use English for all descriptions.

Strict Guidelines:

Preserve the Essence: You MUST start the new '.ai-context.md' with the 'Project Essence' exactly as defined in '.ai-readme.md'.

Architecture Audit: * Verify adherence to "Thin Architecture": Scan 'S5Server/Services/' for Extension Methods on DbSet<T>. Confirm NO MediatR or Repository patterns.

Check GraphQL: Ensure HotChocolate resolvers use [Service] injection and [UseProjection]/[UseFiltering] attributes.

Application Map Update (Navigation & Logic):

Frontend: Scan 'Front/src/app/app.routes.ts' to map every URL route to its Angular Component.

GraphQL: For each component, identify the associated GraphQL Query or Mutation.

Backend: Locate the specific Service or Extension Method in 'S5Server/Services/' that handles the business logic for those calls.

Database Mapping: Scan 'S5Server/Data/Models/' to identify core entity groups and link them to their respective schemas (core, docs, history, etc.).

Refine Tech Stack: Verify current versions in '.csproj' (.NET 10, EF Core 10) and 'package.json' (Angular 21).

UI Policy: Re-confirm "Angular Material ONLY". If you find Любые Bootstrap или Tailwind классы, flag them as technical debt.

Deliverable Structure for .ai-context.md:

Project Map (Permanent Essence & Overview)
Architecture (Thin Backend, GraphQL, Angular Signals)
Tech Stack (Verified versions & libraries)
Application Map (Detailed Table: Route -> Component -> GraphQL -> Backend Logic)
Project Structure (Key directories & File associations)
Infrastructure & Security (K8s, CNPG, JWT, CORS)
Execution: Analyze all project files, detect changes in routes or logic, and output the complete updated content for '.ai-context.md'.